package osmo.tester.model.dataflow;

/**
 * Supported datatypes, data-flow objects can be configured to produce one type based on given type information.
 *
 * @author Teemu Kanstren
 */
public enum DataType {
  INT, LONG, DOUBLE
}
